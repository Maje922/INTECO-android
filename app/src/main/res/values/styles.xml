<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/azulMasOscuro</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="modificacionCalendario" parent="android:Widget.CalendarView">
        <item name="android:selectedWeekBackgroundColor">@color/transparente</item>
        <item name="android:color">@color/azulInteco</item>
    </style>
    
    

</resources>
